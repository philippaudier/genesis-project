# Current

> Live working state only. **Not** history (→ `docs/Journal/`), **not** cross-session memory, **not**
> philosophy (→ `CLAUDE.md`). Keep it short. Update as things change; delete what is no longer current.

- **Genesis-012 First Standing Cause: COMPLETE** — 70/70 EditMode tests green. Proof statement now true: *Genesis-012 proved that declared heterogeneous state can sustain the first recognisable world cause — the world contains a cause of its own change.* The kernel runs re-keyed under RFC-0003 (Place · Kind · Cell); all 004–011 proofs preserved; the standing cause lives entirely in tests; R is never written (causal asymmetry is structural).
- **The Glossary editorial act is done:** **Source** is admitted as the first Active world category (new "Active — world categories" section: predicate, theorem, signature/ablation, standing; compression justified). Stale Address-era entries updated to Place/Kind/Cell; Conflict and Scope entries now cell/kind-precise. *Entity remains Reserved* — no continuity-theorem exists yet.
- **State of the project:** Constitution → ADRs (0001 architecture, 0002 methodology) → RFCs (0001 tick, 0002→ADR-0001, 0003 representation — all Accepted/Resolved) → kernel (Place/Kind/Cell, scoped views, per-kind resolvers) → phenomena (redistribution, production) → first earned word (Source). Every layer speaks only for itself.
- **Next (open, undesigned — await direction):** natural candidates, none chosen: the **Sink** (Source's dual → through-flow, first sustained non-equilibrium dynamic; accounting generalises to `ΔTotal = ΣProduction − ΣConsumption`) · the **constraint layer** (the triad's third element) · larger topologies (dense-field behaviour, W1's real test) · presentation bridge (first observation of a running world from Unity).
- **Standing opens:** constraint layer · numeric-representation RFC · event-scheduling vigilance · topology placement · address lifecycle · write-scoping (parked) · type-level commutativity/scope enforcement · resolver-algebra conjecture · observational-tests family (noticed, not formalised) · production-law universality is fixture convention, not kernel mechanism.
- **Active RFCs:** RFC-0001 Accepted · RFC-0002 Resolved by ADR-0001 · RFC-0003 Accepted.

**Milestone naming convention:** `Genesis-NNN Name`.
