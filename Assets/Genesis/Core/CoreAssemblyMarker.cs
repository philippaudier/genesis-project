namespace Genesis.Core
{
    /// <summary>
    /// Placeholder that establishes the <c>Genesis.Core</c> assembly and namespace for
    /// Genesis-001 (Foundation). It holds no logic. Real types arrive with the milestone that
    /// needs them; this marker only keeps the assembly non-empty so it compiles without warnings.
    /// </summary>
    internal static class CoreAssemblyMarker
    {
    }
}
