# RD-12 probe — competence under length and repeated history

Status: **exploratory, contaminated, non-evidential**.

This probe is downstream of S1-005 and deliberately reuses its candidate
competence fixture, integer values, and forcing scale. Nothing it produces may
become an Observation or campaign evidence.

It asks only where the next resistance lives. Bidirectional chains of 2, 3, 5,
and 9 places receive three repetitions of the S1-005 forcing episode:

```text
+8 Water at the left end on boundaries 0,1; 8,9; 16,17
```

Every place begins with Base 0, Rock 8, Sediment 0, Water 0. The first pass
retains S1-005's constant divisor 2 and strict conversion threshold 3. The
strict comparison remains competence 0 versus competence 1.

The first pass exposed negative, unrepeated trajectories from length 5
onward. A second diagnostic pass therefore repeats the same contaminated
matrix with the corpus's existing degree-aware divisor (`degree + 1`). This
does not add a campaign factor; it asks whether the resistance belongs to
competence or to the already-known carrier-law instability.

The probe watches through tick 256 and searches for the first complete-state
recurrence only after the last crossing has entered. It distinguishes:

- non-uniform fixed point;
- flat fixed point;
- material orbit;
- no recurrence in the window;
- conservation failure;
- and the first negative Kind, place, tick, and value.

The two-place row is an internal reproduction check, not a new specimen.
Every other row is contaminated by this exploratory search and unavailable to
a future campaign.
